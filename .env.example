# ═══════════════════════════════════════════════════════════
# VARIABLES D'ENVIRONNEMENT POUR RAILWAY
# Copiez ce fichier et ajoutez ces variables dans Railway
# ═══════════════════════════════════════════════════════════

# ── DATABASE (obligatoire pour PostgreSQL) ──
# Railway fournit automatiquement DATABASE_URL quand vous ajoutez une base PostgreSQL
# Format: postgresql://username:password@host:port/database
# Exemple: DATABASE_URL=postgresql://postgres:password@postgres.railway.internal:5432/railway
DATABASE_URL=${{Postgres.DATABASE_URL}}

# ── SECURITY (obligatoire) ──
# Générez une clé secrète forte (au moins 32 caractères aléatoires)
SECRET_KEY=changez-moi-par-une-cle-secrete-forte-et-aleatoire-32-chars-minimum

# ── ARDUINO (optionnel) ──
# Secret pour sécuriser les communications Arduino
ARDUINO_SECRET=babyfoot-arduino-secret-2024-changez-moi

# ── PORT (automatique Railway) ──
# Railway définit automatiquement $PORT, ne pas modifier
# PORT=8080

# ═══════════════════════════════════════════════════════════
# COMMENT CONFIGURER DANS RAILWAY :
# ═══════════════════════════════════════════════════════════
# 
# 1. Créer une base de données PostgreSQL dans Railway
#    - Cliquez sur "+ New" → "Database" → "Add PostgreSQL"
#    - Railway va créer automatiquement DATABASE_URL
# 
# 2. Dans votre projet (service), allez dans "Variables"
#    - Ajoutez SECRET_KEY avec une valeur forte
#    - Ajoutez ARDUINO_SECRET (optionnel)
#    - DATABASE_URL sera déjà présent si vous avez lié la DB
# 
# 3. Pour lier la base de données à votre service :
#    - Dans votre service, cliquez sur "Variables"
#    - Cliquez sur "+ New Variable" → "Reference"
#    - Sélectionnez "Postgres" → "DATABASE_URL"
#    - Ou utilisez: ${{Postgres.DATABASE_URL}}
# 
# ═══════════════════════════════════════════════════════════
